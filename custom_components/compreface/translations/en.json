{
  "config": {
    "step": {
      "user": {
        "title": "CompreFace configuration",
        "description": "If you need help with the configuration have a look here: https://github.com/frnyb-home-assistant/compreface",
        "data": {
          "host": "CompreFace host IP address",
          "port": "CompreFace host Port",
          "recognize_api_key": "Face recognition API key",
          "detect_api_key": "Face detection API key",
          "verify_api_key": "Face verification API key",
          "image_temp_dir": "Temporary directory for images"
        },
        "data_description": {
          "recognize_api_key": "Leave empty if you don't want to setup face recognition",
          "detect_api_key": "Leave empty if you don't want to setup face detection",
          "verify_api_key": "Leave empty if you don't want to setup face verification",
          "image_temp_dir": "Directory must be accessible by the Home Assistant user, list dir in allowlist_external_dirs in configuration.yaml"
        }
      }
    },
    "error": {
      "invalid_host": "Host IP is invalid.",
      "empty_keys": "At least one API key must be provided.",
      "invalid_temp_dir": "Temporary directory can not be accessed."
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  },
  "options": {
    "step": {
      "recognition": {
        "title": "Face recognition setup",
        "data": {
          "recognition_camera_devices": "Cameras to bind face recognition to"
        }
      },
      "verification": {
        "title": "Face verification setup",
        "data": {
          "verification_camera_devices": "Cameras to bind face verification to"
        }
      },
      "detection": {
        "title": "Face detection setup",
        "data": {
          "detection_camera_devices": "Cameras to bind face detection to"
        }
      }
    }
  }
}
